import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.campusfix.student',
  appName: 'CampusFix Student',
  webDir: 'dist-capacitor'
};

export default config;
