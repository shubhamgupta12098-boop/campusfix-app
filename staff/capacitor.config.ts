import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.campusfix.staff',
  appName: 'CampusFix Staff',
  webDir: 'dist-capacitor'
};

export default config;
