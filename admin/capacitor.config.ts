import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.campusfix.admin',
  appName: 'CampusFix Admin',
  webDir: 'dist-capacitor'
};

export default config;
