# Android API connection fix

`CapacitorHttp` native HTTP patch enable kiya gaya hai. Isse Android app ki fetch requests native networking layer se jayengi.

## Commands

```powershell
npm install
npm run build
npx cap sync android
npx cap open android
```

Phone/emulator se purani CampusFix app uninstall karke nayi APK install karein.
