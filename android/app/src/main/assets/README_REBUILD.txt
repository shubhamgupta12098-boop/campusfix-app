The bundled web assets were intentionally omitted from the updated package.

From the project root, run:

  npm run build
  npx cap sync android

This regenerates the Android web bundle from the updated full-stack source.
